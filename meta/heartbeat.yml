# This file is intentionally kept as a tombstone.
#
# The heartbeat workflow was previously placed here (meta/heartbeat.yml)
# but GitHub Actions never reads workflows outside .github/workflows/.
# The live workflow is now at:
#
#   .github/workflows/heartbeat.yml
#
# Do not add workflow YAML to this directory.
