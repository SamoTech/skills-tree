# Badge JSONs

This directory is served via GitHub Pages from `main`.

Badge JSON files are auto-generated here by `ast-sweep.yml` and `sync-badges.yml`.

Endpoint format: `https://samotech.github.io/skills-tree/badges/{key}.json`
