# Runtime dependencies for GitHub Actions workflows.
# ALL packages pinned to exact versions for reproducibility.
# Bump via Dependabot (weekly) or manually after testing.
# Last verified: 2026-06-14
httpx==0.28.1
PyYAML==6.0.3
PyGithub==2.9.1
python-dateutil==2.9.0.post0
jsonschema==4.26.0

# SEC-2 FIX (2026-06-14): requests added so Dependabot monitors it.
# Previously installed inline via `pip install requests` in osv-watch.yml —
# outside Dependabot's visibility. osv_watch_scan.py now uses httpx instead;
# this entry ensures any future CVEs in requests are still reported if
# other scripts use it.
requests==2.33.0

# Test dependencies — used by pytest in CI (test.yml) and locally.
pytest==9.1.0
pytest-asyncio==0.25.3
pytest-cov==6.0.0
pytest-xdist==3.6.1
