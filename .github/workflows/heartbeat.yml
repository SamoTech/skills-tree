name: Heartbeat

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions: {}

jobs:
  ping:
    name: Repository heartbeat
    runs-on: ubuntu-latest
    permissions:
      contents: read
