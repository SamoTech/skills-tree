# sync-readme-badges.yml
# DEPRECATED — superseded by update-skill-count.yml which has a proper
# retry-rebase loop.  This file is intentionally disabled to eliminate
# the duplicate push-conflict race.  Do not re-enable without removing
# update-skill-count.yml first.
name: 🔢 Sync Skill Count Badges (DISABLED)

on:
  workflow_dispatch:   # manual only — no automatic triggers

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use update-skill-count.yml instead."
