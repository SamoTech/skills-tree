name: Taxonomy Integration Verification

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/verify-taxonomy.yml'
      - 'tools/verify_taxonomy.py'

# Least-privilege permissions (CodeQL: "Workflow does not contain permissions")
# This workflow runs a read-only Python verification script.
# It does not modify the repository, create issues, or post comments.
permissions:
  contents: read          # required by actions/checkout to clone the repo

jobs:
  verify:
    name: Runtime Taxonomy Verification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Run verification script
        run: python tools/verify_taxonomy.py

      - name: Upload verification results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taxonomy-verification-results
          path: |
            verify_before.json
            verify_after.json
            verify_diff.txt
            verify_result.txt
