name: Schema Enforce

on:
  pull_request:
    paths:
      - 'skills/**/*.md'
      - 'meta/schema/**'

permissions: {}

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
