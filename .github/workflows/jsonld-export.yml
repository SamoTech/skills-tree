name: JSON-LD Export

on:
  push:
    paths:
      - "skills/**"
  schedule:
    # Every Monday at 04:00 UTC
    - cron: "0 4 * * 1"
  workflow_dispatch:

# CRITICAL-2 fix: shared concurrency group serialises all bot-commit workflows
# that push to main. cancel-in-progress: false ensures no push is silently
# dropped — each run queues and executes in order.
concurrency:
  group: auto-commit-main
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pyyaml

      - name: Run export (JSON, YAML, Schema, JSON-LD)
        run: python3 tools/export_skills.py

      - name: Commit generated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/api/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore(api): auto-update JSON, YAML, JSON-LD exports [skip ci]"
            git pull --rebase origin main
            git push
          fi
