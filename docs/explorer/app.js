'use strict';
const GRAPH_URL='../../data/SKILLS_GRAPH.json';
const GITHUB_BASE='https://github.com/SamoTech/skills-tree/blob/main/';
const CAT_LABELS={'00-sandbox':'00 Sandbox','01-perception':'01 Perception','02-reasoning':'02 Reasoning','03-memory':'03 Memory','04-action-execution':'04 Action','05-code':'05 Code','06-communication':'06 Comm','07-tool-use':'07 Tools','08-multimodal':'08 Multimodal','09-agentic-patterns':'09 Agentic','10-computer-use':'10 CU','11-web':'11 Web','12-data':'12 Data','13-safety':'13 Safety'};
let graph=null,nodes=[],edges=[],filteredNodes=[],selectedId=null;
let activeFilters={level:'all',stability:'all',category:'all'},searchQuery='';
let edgesBySource={},edgesByTarget={},nodeById={};
(function initTheme(){const btn=document.querySelector('[data-theme-toggle]');const html=document.documentElement;const dark=window.matchMedia('(prefers-color-scheme: dark)').matches;let theme=dark?'dark':'light';html.setAttribute('data-theme',theme);updateThemeIcon(btn,theme);if(btn){btn.addEventListener('click',()=>{theme=theme==='dark'?'light':'dark';html.setAttribute('data-theme',theme);updateThemeIcon(btn,theme)});}})();
function updateThemeIcon(btn,theme){if(!btn)return;btn.setAttribute('aria-label',`Switch to ${theme==='dark'?'light':'dark'} mode`);btn.innerHTML=theme==='dark'?'<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="12" cy="12" r="5"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>':'<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>';}
document.addEventListener('DOMContentLoaded',()=>{loadGraph();setupSearch();setupFilters();setupURLRouting();setupKeyboardShortcut();setupMobileOverlayClose();});
async function loadGraph(){try{const res=await fetch(GRAPH_URL);if(!res.ok)throw new Error(`HTTP ${res.status}`);graph=await res.json();nodes=graph.nodes||[];edges=graph.edges||[];nodeById={};edgesBySource={};edgesByTarget={};for(const n of nodes){nodeById[n.id]=n;}for(const e of edges){if(!edgesBySource[e.source])edgesBySource[e.source]=[];edgesBySource[e.source].push(e);if(!edgesByTarget[e.target])edgesByTarget[e.target]=[];edgesByTarget[e.target].push(e);}updateMetrics();buildCategoryFilters();applyFilters();handleURLParam();}catch(err){showError(err);}}
function updateMetrics(){const meta=graph.meta||{};const cats=new Set(nodes.map(n=>n.category));const reqEdges=edges.filter(e=>e.type==='REQUIRES');animateNumber('metric-nodes',meta.node_count||nodes.length);animateNumber('metric-edges',meta.edge_count||edges.length);animateNumber('metric-cats',cats.size);animateNumber('metric-requires',meta.requires_count||reqEdges.length);document.getElementById('metric-schema').textContent=meta.schema_version||'—';}
function animateNumber(id,target){const el=document.getElementById(id);if(!el)return;let current=0;const step=Math.ceil(target/40);const timer=setInterval(()=>{current=Math.min(current+step,target);el.textContent=current.toLocaleString();if(current>=target)clearInterval(timer);},16);}
function buildCategoryFilters(){const container=document.getElementById('filter-category');const cats=[...new Set(nodes.map(n=>n.category))].sort();const allChip=document.createElement('button');allChip.className='chip chip-active';allChip.dataset.filter='category';allChip.dataset.value='all';allChip.setAttribute('aria-pressed','true');allChip.textContent='All';container.appendChild(allChip);for(const cat of cats){const btn=document.createElement('button');btn.className='chip';btn.dataset.filter='category';btn.dataset.value=cat;btn.setAttribute('aria-pressed','false');btn.textContent=CAT_LABELS[cat]||cat;container.appendChild(btn);}}
function setupSearch(){const input=document.getElementById('skill-search');input.addEventListener('input',(e)=>{searchQuery=e.target.value.trim().toLowerCase();applyFilters();});}
function setupKeyboardShortcut(){document.addEventListener('keydown',(e)=>{if(e.key==='/'&&document.activeElement.tagName!=='INPUT'){e.preventDefault();document.getElementById('skill-search').focus();}if(e.key==='Escape'){closeMobileOverlay();}});}
function setupFilters(){document.querySelectorAll('.chip[data-filter]').forEach(btn=>{btn.addEventListener('click',()=>{const filter=btn.dataset.filter;const value=btn.dataset.value;activeFilters[filter]=value;const group=btn.closest('.filter-chips')||document.getElementById('filter-category');group.querySelectorAll('.chip').forEach(c=>{const active=c.dataset.value===value;c.classList.toggle('chip-active',active);c.setAttribute('aria-pressed',active?'true':'false');});applyFilters();});});document.getElementById('filter-category').addEventListener('click',(e)=>{const btn=e.target.closest('.chip[data-filter="category"]');if(!btn)return;const value=btn.dataset.value;activeFilters.category=value;document.querySelectorAll('#filter-category .chip').forEach(c=>{const active=c.dataset.value===value;c.classList.toggle('chip-active',active);c.setAttribute('aria-pressed',active?'true':'false');});applyFilters();});}
function applyFilters(){filteredNodes=nodes.filter(n=>{if(n.category==='00-sandbox')return false;if(activeFilters.level!=='all'&&n.level!==activeFilters.level)return false;if(activeFilters.stability!=='all'&&n.stability!==activeFilters.stability)return false;if(activeFilters.category!=='all'&&n.category!==activeFilters.category)return false;if(searchQuery){const haystack=`${n.title} ${n.id} ${n.category} ${(n.tags||[]).join(' ')}`.toLowerCase();if(!haystack.includes(searchQuery))return false;}return true;});filteredNodes.sort((a,b)=>{if(a.id===selectedId)return -1;if(b.id===selectedId)return 1;return a.title.localeCompare(b.title);});renderGrid();document.getElementById('results-count').textContent=`${filteredNodes.length.toLocaleString()} skill${filteredNodes.length!==1?'s':''}`;}
function renderGrid(){const grid=document.getElementById('skill-grid');grid.innerHTML='';if(filteredNodes.length===0){grid.innerHTML='<div class="empty-state" role="status"><svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true" style="color:var(--color-text-faint)"><circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/></svg><p class="empty-state-title">No skills found</p><p>Try a different search or clear your filters.</p></div>';return;}const fragment=document.createDocumentFragment();for(const node of filteredNodes){fragment.appendChild(createSkillCard(node));}grid.appendChild(fragment);}
function createSkillCard(node){const card=document.createElement('div');card.className='skill-card'+(node.id===selectedId?' selected':'');card.setAttribute('role','listitem');card.setAttribute('tabindex','0');card.setAttribute('aria-label',`${node.title}, ${node.level}, ${node.stability}`);card.dataset.id=node.id;const title=searchQuery?highlightText(node.title,searchQuery):escHtml(node.title);card.innerHTML=`<div class="skill-card-title">${title}</div><div class="skill-card-row"><span class="badge badge-level-${node.level}">${node.level}</span><span class="badge badge-${node.stability}">${node.stability}</span><span class="skill-card-id">${escHtml(node.id)}</span></div>`;card.addEventListener('click',()=>selectSkill(node.id));card.addEventListener('keydown',(e)=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();selectSkill(node.id);}});return card;}
function highlightText(text,query){const escaped=escHtml(text);const escapedQuery=escHtml(query).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');return escaped.replace(new RegExp(`(${escapedQuery})`,'gi'),'<mark>$1</mark>');}
function escHtml(s){return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
function selectSkill(id){selectedId=id;const node=nodeById[id];if(!node)return;const url=new URL(window.location.href);url.searchParams.set('skill',id);window.history.pushState({skill:id},'',url.toString());document.querySelectorAll('.skill-card').forEach(c=>{c.classList.toggle('selected',c.dataset.id===id);});renderDetail(node);}
function renderDetail(node){const empty=document.getElementById('detail-empty');const content=document.getElementById('detail-content');empty.hidden=true;content.hidden=false;document.getElementById('d-cat').textContent=node.category;document.getElementById('d-title').textContent=node.title;document.getElementById('d-id').textContent=node.id;document.getElementById('d-level').textContent=node.level||'—';document.getElementById('d-stability').textContent=node.stability||'—';document.getElementById('d-version').textContent=node.version||'—';document.getElementById('d-layer').textContent=node.layer||'—';const levelEl=document.getElementById('d-level');levelEl.className=`tag badge-level-${node.level}`;const stabEl=document.getElementById('d-stability');stabEl.className=`tag badge-${node.stability}`;document.getElementById('d-added').textContent=node.added||'—';document.getElementById('d-source').textContent=node.source_file||'—';const ghBtn=document.getElementById('btn-github');if(node.source_file){ghBtn.onclick=()=>window.open(GITHUB_BASE+node.source_file,'_blank','noopener');ghBtn.style.display='';}else{ghBtn.style.display='none';}document.getElementById('btn-share').onclick=()=>{const shareUrl=new URL(window.location.href);shareUrl.searchParams.set('skill',node.id);navigator.clipboard.writeText(shareUrl.toString()).then(()=>showToast('🔗 Link copied!'));};renderDepList('d-prereqs','section-prereqs',node.prerequisites||[],'prereq');const reqOut=(edgesBySource[node.id]||[]).filter(e=>e.type==='REQUIRES').map(e=>e.target);renderDepList('d-requires-out','section-requires-out',reqOut,'req-out');const reqIn=(edgesByTarget[node.id]||[]).filter(e=>e.type==='REQUIRES').map(e=>e.source);renderDepList('d-requires-in','section-requires-in',reqIn,'req-in');renderDepList('d-related','section-related',node.related_skills||[],'related');const isMobile=window.innerWidth<=900;if(isMobile){const overlay=document.getElementById('mobile-overlay');const body=document.getElementById('mobile-overlay-body');body.innerHTML=document.getElementById('detail-content').outerHTML;body.querySelectorAll('.dep-item').forEach(item=>{item.addEventListener('click',()=>selectSkill(item.dataset.id));});const shareBtn2=body.querySelector('#btn-share');if(shareBtn2)shareBtn2.onclick=document.getElementById('btn-share').onclick;const ghBtn2=body.querySelector('#btn-github');if(ghBtn2&&node.source_file)ghBtn2.onclick=()=>window.open(GITHUB_BASE+node.source_file,'_blank','noopener');overlay.removeAttribute('hidden');overlay.focus();}}
function renderDepList(listId,sectionId,ids,type){const list=document.getElementById(listId);const section=document.getElementById(sectionId);if(!ids.length){section.style.display='none';return;}section.style.display='';list.innerHTML='';for(const id of ids){const n=nodeById[id];const label=n?n.title:id;const item=document.createElement('div');item.className='dep-item';item.dataset.id=id;item.setAttribute('role','button');item.setAttribute('tabindex','0');item.setAttribute('aria-label',`Go to ${label}`);item.innerHTML=`<svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>${escHtml(label)}`;item.addEventListener('click',()=>{selectSkill(id);scrollToCard(id);});item.addEventListener('keydown',e=>{if(e.key==='Enter'){selectSkill(id);scrollToCard(id);}});list.appendChild(item);}}
function scrollToCard(id){const card=document.querySelector(`.skill-card[data-id="${CSS.escape(id)}"]`);if(card)card.scrollIntoView({behavior:'smooth',block:'nearest'});}
function setupURLRouting(){window.addEventListener('popstate',handleURLParam);}
function handleURLParam(){const params=new URLSearchParams(window.location.search);const skillId=params.get('skill');if(skillId&&nodeById[skillId]){selectSkill(skillId);}}
function setupMobileOverlayClose(){document.getElementById('close-overlay').addEventListener('click',closeMobileOverlay);}
function closeMobileOverlay(){document.getElementById('mobile-overlay').setAttribute('hidden','');}
function showToast(msg){const toast=document.getElementById('toast');toast.textContent=msg;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200);}
function showError(err){console.error('[Explorer] Failed to load graph:',err);document.getElementById('skill-grid').innerHTML='<div class="empty-state" role="alert"><p class="empty-state-title">Failed to load skills graph</p><p>Could not fetch <code>data/SKILLS_GRAPH.json</code>.<br>Running locally? Use: <code>python3 -m http.server</code></p></div>';document.getElementById('results-count').textContent='Error loading data';}